import { PreferencesPipe } from './preferences.pipe';

describe('PreferencesPipe', () => {
  it('create an instance', () => {
    const pipe = new PreferencesPipe();
    expect(pipe).toBeTruthy();
  });
});
